public class MovieReport extends Report {
    public MovieReport(ReportType reportType, String morDetail, User reporter, ReportStatus reportStatus) {
        super(reportType, morDetail, reporter, reportStatus);
    }

}
