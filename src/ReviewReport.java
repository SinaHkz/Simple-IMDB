public class ReviewReport extends Report{
    public ReviewReport(ReportType reportType, String morDetail, User reporter, ReportStatus reportStatus) {
        super(reportType, morDetail, reporter, reportStatus);
    }
}
